import { StyleSheet, Text, View } from "react-native";
import React from "react";
import PayNewCard from "@/src/features/Shopping/screens/pay-new-card";

const PaymentNewCardScreen = () => {
  return <PayNewCard />;
};

export default PaymentNewCardScreen;

const styles = StyleSheet.create({});
