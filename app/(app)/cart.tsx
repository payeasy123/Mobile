import { StyleSheet, Text, View } from "react-native";
import React from "react";
import Cart from "@/src/features/Shopping/screens/cart";

const CartScreen = () => {
  return <Cart />;
};

export default CartScreen;

const styles = StyleSheet.create({});
