import { StyleSheet, Text, View } from "react-native";
import React from "react";
import PayMyCard from "@/src/features/Shopping/screens/pay-one";

const PaymentScreen = () => {
  return <PayMyCard />;
};

export default PaymentScreen;

const styles = StyleSheet.create({});
